// This TypeScript file is imported in index.html and automatically transpiled by Parcel.
// This is only here as an example to demonstrate that Parcel is doing what it does best.
const foo: string = 'TypeScript';
console.log(`${foo} is also working`);
